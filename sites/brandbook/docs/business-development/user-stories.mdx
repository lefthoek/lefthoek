---
name: User Stories
menu: Business Development
route: /user-stories
---
import { UserStory } from "@lefthoek/business-components"
import { userStories} from "@lefthoek/data"

### User Stories

<div>{
 Object.entries(userStories).sort(([_, storyA], [__, storyB]) => storyB.rating - storyA.rating).map(([_, story]) => <UserStory {...story} />)
}</div>
