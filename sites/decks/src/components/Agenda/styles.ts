const innerWrapper = {
  display: "grid",
  alignContent: "center",
};
const outerWrapper = {
  display: "grid",
  gridTemplateRows: ["14fr 1fr"],
};

const logoStyles = {};

export { outerWrapper, innerWrapper, logoStyles };
