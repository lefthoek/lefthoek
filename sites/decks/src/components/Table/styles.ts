const innerWrapper = {
  display: "grid",
  alignContent: "center",
  justifyContent: "stretch",
};

const outerWrapper = {
  display: "grid",
  gridTemplateRows: ["14fr 1fr"],
};

const logoStyles = {};

export { outerWrapper, innerWrapper, logoStyles };
