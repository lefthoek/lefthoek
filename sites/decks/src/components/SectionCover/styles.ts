const outerWrapper = {
  display: "grid",
};

const innerWrapper = {
  display: "grid",
  alignContent: "center",
  h1: {
    textAlign: ["center", "center"],
    fontSize: [7, 8],
  },
};

export { innerWrapper, outerWrapper };
